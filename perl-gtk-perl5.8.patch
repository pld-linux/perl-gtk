--- Gtk-Perl-0.7008/Gtk/Makefile.PL.orig	Sat May 26 18:08:38 2001
+++ Gtk-Perl-0.7008/Gtk/Makefile.PL	Thu Mar 20 20:05:14 2003
@@ -190,7 +190,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/Applet/Makefile.PL.orig	Sat Mar 24 17:44:08 2001
+++ Gtk-Perl-0.7008/Applet/Makefile.PL	Thu Mar 20 20:06:55 2003
@@ -102,7 +102,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/Bonobo/Makefile.PL.orig	Wed May 30 15:39:05 2001
+++ Gtk-Perl-0.7008/Bonobo/Makefile.PL	Thu Mar 20 20:07:22 2003
@@ -96,7 +96,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/GdkImlib/Makefile.PL.orig	Fri Mar 23 15:57:07 2001
+++ Gtk-Perl-0.7008/GdkImlib/Makefile.PL	Thu Mar 20 20:07:57 2003
@@ -43,6 +43,6 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
--- Gtk-Perl-0.7008/GdkPixbuf/Makefile.PL.orig	Fri Mar 23 15:57:07 2001
+++ Gtk-Perl-0.7008/GdkPixbuf/Makefile.PL	Thu Mar 20 20:08:30 2003
@@ -89,6 +89,6 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
--- Gtk-Perl-0.7008/Glade/Makefile.PL.orig	Fri Mar 23 15:57:07 2001
+++ Gtk-Perl-0.7008/Glade/Makefile.PL	Thu Mar 20 20:09:06 2003
@@ -108,7 +108,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/Gnome/Makefile.PL.orig	Fri Mar 23 15:57:08 2001
+++ Gtk-Perl-0.7008/Gnome/Makefile.PL	Thu Mar 20 20:10:27 2003
@@ -125,7 +125,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/GnomePrint/Makefile.PL.orig	Fri Mar 23 15:57:08 2001
+++ Gtk-Perl-0.7008/GnomePrint/Makefile.PL	Thu Mar 20 20:10:48 2003
@@ -112,7 +112,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/GtkGLArea/Makefile.PL.orig	Tue Mar 18 20:38:36 2003
+++ Gtk-Perl-0.7008/GtkGLArea/Makefile.PL	Thu Mar 20 20:11:26 2003
@@ -104,7 +104,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/GtkHTML/Makefile.PL.orig	Fri Mar 23 15:57:08 2001
+++ Gtk-Perl-0.7008/GtkHTML/Makefile.PL	Thu Mar 20 20:12:41 2003
@@ -107,7 +107,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/GtkXmHTML/Makefile.PL.orig	Fri Mar 23 15:57:08 2001
+++ Gtk-Perl-0.7008/GtkXmHTML/Makefile.PL	Thu Mar 20 20:14:13 2003
@@ -95,7 +95,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
--- Gtk-Perl-0.7008/Mozilla/Makefile.PL.orig	Fri Mar 23 15:57:08 2001
+++ Gtk-Perl-0.7008/Mozilla/Makefile.PL	Thu Mar 20 20:14:44 2003
@@ -94,7 +94,7 @@
 sub MY::c_o {
 	package MY; # so that "SUPER" works right
 	my $inherited = shift->SUPER::c_o(@_);
-	$inherited =~ s/CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
+	$inherited =~ s/^\.c\$\(OBJ_EXT\):\n.*CCCMD.*$/$&\n\t\@if test -f `basename \$*.o` -a "`basename \$*.o`" != "\$*.o"; then mv `basename \$*.o` \$*.o; fi/m;
     $inherited;
 }
 
